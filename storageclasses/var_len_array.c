#include<stdio.h>
int main()
{

	size_t size=0;
	printf("Enter the no of elements u want to store:\n");
	scanf("%zd",&size);
	int value[size];//allocating size at exec time rather than at comp time


}
